name: Daily Batch

on:
  schedule:
    - cron: '0 18 * * 1-5'  # UTC 18:00 = KST 03:00 (다음날 새벽), weekdays
  workflow_dispatch:
    inputs:
      force_reanalysis:
        description: 'Skip 24h cache and re-analyze all (1=force)'
        required: false
        default: '0'
      bulk_batch_max:
        description: 'Max stocks to analyze (default 500 = all)'
        required: false
        default: '500'
jobs:
  watchlist-reanalysis:
    name: Watchlist Re-analysis
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
          cache-dependency-path: backend/requirements.txt

      - name: Install dependencies
        working-directory: backend
        run: pip install -r requirements.txt

      - name: Run watchlist batch
        working-directory: backend
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_KEY: ${{ secrets.SUPABASE_SERVICE_KEY }}
          YOUTUBE_API_KEY: ${{ secrets.YOUTUBE_API_KEY }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          RESEND_API_KEY: ${{ secrets.RESEND_API_KEY }}
        run: python -m app.jobs.watchlist_batch

  supply-collection:
    name: Watchlist Supply Data Collection
    runs-on: ubuntu-latest
    needs: watchlist-reanalysis
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
          cache-dependency-path: backend/requirements.txt

      - name: Install dependencies
        working-directory: backend
        run: pip install -r requirements.txt

      - name: Collect supply data for watchlist
        working-directory: backend
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_KEY: ${{ secrets.SUPABASE_SERVICE_KEY }}
        run: python -m app.jobs.supply_collect_batch

  congress-collection:
    name: Congress Trades Collection
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
          cache-dependency-path: backend/requirements.txt

      - name: Install dependencies
        working-directory: backend
        run: pip install -r requirements.txt

      - name: Collect congressional trades
        working-directory: backend
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_KEY: ${{ secrets.SUPABASE_SERVICE_KEY }}
        run: python -m app.jobs.congress_collect_batch

  bulk-analysis:
    name: Bulk Analysis + Ranking Snapshot
    runs-on: ubuntu-latest
    needs: watchlist-reanalysis
    timeout-minutes: 120
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
          cache-dependency-path: backend/requirements.txt

      - name: Install dependencies
        working-directory: backend
        run: pip install -r requirements.txt

      - name: Run bulk analysis + ranking snapshot
        working-directory: backend
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_KEY: ${{ secrets.SUPABASE_SERVICE_KEY }}
          YOUTUBE_API_KEY: ${{ secrets.YOUTUBE_API_KEY }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          BULK_BATCH_MAX: ${{ github.event.inputs.bulk_batch_max || '500' }}
          FORCE_REANALYSIS: ${{ github.event.inputs.force_reanalysis || '0' }}
        run: python -m app.jobs.bulk_analysis_batch
